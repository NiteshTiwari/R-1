# Bug Testing ARMA_MODEL_TESTNG
# mkil

test.diff <- coredata(sc.data[360])-coredata(sc.data[359])
print(test.diff)
print(tail(actual.diff,1))

print(dirname(parent.frame(2)$ofile))
print(dirname(parent.frame(2)$ofile))